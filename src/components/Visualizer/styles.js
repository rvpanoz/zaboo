const styles = theme => ({
  root: {
    display: "flex",
    flexDirection: "column",
    width: "100%",
    height: "100%"
  },
  visualizer: {
    flexGrow: 1
  },
  audio: {
    textAlign: "center"
  }
});

export default styles;
